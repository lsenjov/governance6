# Dice rolls (skill + chaos) on top-of-queue calls — v3

## Changes from v2

- **Universal "natural 1" rule.** Any die showing a `1` in _any_ slot
  — skill, chaos, or any future extras — is treated as a failure and
  rendered with the failure visual treatment.
- **Persisted at the data layer, not just the UI.** v2 left chaos and
  extras either neutral or caller-controlled. v3 introduces a
  `chaosResult: v.optional(v.union(success, failure))` field, enforces
  `chaosRoll === 1 → "failure"` in the roll-generation helper, and
  forces every extras item with `value === 1` to record
  `result: "failure"` regardless of what the caller passed.
- **Skill rule restated explicitly.** The skill failure formula
  becomes `skillRoll === 1 || skillRoll <= skillCount` (the `=== 1`
  branch is currently redundant because Minions are required to carry
  1–5 skills per `convex/schema.ts:61-63`, but stating it makes the
  rule self-documenting and forward-compatible if the skill-count
  invariant ever loosens).
- **Defence-in-depth in the UI.** `RollSetDisplay` re-applies the
  natural-1 rule when rendering, so even a row written before this
  feature shipped (or by any future code path that bypasses the
  helper) still highlights correctly.
- New tests for chaos and extras failure derivation, and a new
  verification criterion for natural-1s.

Touched: Key findings 7–8, Tasks 1, 3, 7–9, 12, 16, 19, plus a new
verification criterion and risk entry. v1/v2 changelog retained for
audit.

## Changes from v1 (carried forward)

- Each entry in `callRollSets.extras` carries a **required**
  human-readable `name: v.string()` (in addition to the machine
  discriminator `kind`).
- `name` is trimmed and 1–24 characters, validated at insert time
  inside the roll-generation helper.

## Objective

Introduce a GM-only dice-roll system that fires automatically whenever a
Call reaches the head of the FIFO queue (Rule 22, `convex/calls.ts:11-19`).
Each event produces at minimum two six-sided rolls:

- **Skill roll** — compared against the called Minion's skill count
  (`minions.skills.length`). A roll **≤** that count is a **failure**;
  a roll **>** that count is a **success**. Additionally, a natural
  `1` is always a failure regardless of skill count.
- **Chaos roll** — a plain d6. A natural `1` is a failure; any other
  value is rendered neutrally.

Universally, **any die in any slot showing a `1` is highlighted as a
failure**. This applies to skill, chaos, and every present-or-future
extras die.

The rolls are visible **only to the GM**, regardless of where they appear
(the right-rail Call Queue, the main-column Current Call section, or
inline next to a Note). The data model must accommodate "more rolls
added conditionally in the future" without further schema churn — and
**every additional roll, present or future, must be displayed with a
human-readable name** so the GM never sees an unlabelled die.

When a Note is created against the Minion that is currently at the top
of the queue, the active roll set is **frozen onto the Note**, so the
GM can later read the Notes log and see which rolls were live at the
moment the Note was made — even after the call has been removed and
the rolls would otherwise be gone.

Visually, dice render as brutalist square cells: hard 2px black border,
zero radius, big mono numerals (no pip dots, no skeuomorphic die faces).
Failure is signalled by riot-red treatment; success uses the existing
mint accent strip; a chaos roll that is not a `1` is presented
neutrally in white. The look continues the system already shipped per
`THEME.md:62-91`.

## Project structure summary

- **Call Queue (Rule 22).** FIFO head is computed via the
  `by_game_active_time` index in `convex/calls.ts:114-120` and reused
  by `getCurrentCallDetails` at `convex/calls.ts:151-209`. Mutations
  that mutate the head are `addOrReplaceCall`
  (`convex/calls.ts:22-70`) and `removeCall`
  (`convex/calls.ts:76-92`).
- **Notes.** Created in `convex/notes.ts:50-131`, listed in
  `convex/notes.ts:158-231`, counted in
  `convex/notes.ts:238-275`. Notes are immutable once created and
  per-game scoped.
- **Auth.** GM-vs-Player gating uses `requireGameGm` and
  `requireGameParticipant` from `convex/lib/auth.ts:53-125`.
- **Schema.** All tables live in `convex/schema.ts`; `calls` at
  `convex/schema.ts:142-154` and `notes` at
  `convex/schema.ts:159-182`. Minions are required to carry 1–5
  skills (`convex/schema.ts:61-63`).
- **Frontend display surfaces** for the head call:
  - Right-rail summary list — `CallQueueRail`
    (`src/pages/GameDetailPage.tsx:1403-1483`).
  - GM main-column deep-dive — `CurrentCallSection`
    (`src/pages/GameDetailPage.tsx:2057-2294`), already subscribes
    GM-only via `api.calls.getCurrentCallDetails`.
  - Mobile bottom sheet — `BottomStrip` (`src/pages/GameDetailPage.tsx:1974-2041`),
    which mirrors the rail.
  - Notes — rendered by the extracted `NoteList` component (re-used
    by `NoteIcon.tsx` popover and the Current Call section's notes
    column at `src/pages/GameDetailPage.tsx:2258-2290`).
- **Theme primitives** available without new CSS:
  `card`, `card tight`, `card.drawback` (riot-red top rule for
  destructive/failure framing — `THEME.md:114-115`,
  `src/index.css:986`), `badge` and `badge.danger`
  (`src/index.css:549-590`), mono/tabular utilities
  (`THEME.md:170-180`).
- **Mutations as the random source.** Convex mutations may use
  `Math.random()` and `Date.now()` (queries may not). All roll
  generation must therefore happen in mutations (`addOrReplaceCall`,
  `removeCall`), not in queries.

## Key findings & rationale

1. **Roll generation is a side-effect of "becoming the head."** The
   only deterministic way to detect a "new head" is at the moment a
   mutation changes the queue: `addOrReplaceCall` (adding to an empty
   queue, or changing the minion of the existing head call) and
   `removeCall` (the next-oldest call inherits the head slot). All
   roll generation belongs in those two mutations via a shared helper.
   This keeps queries pure and live-subscribable.

2. **Replace-in-place needs a new roll set.** `addOrReplaceCall` keeps
   the same `_id` and `createdAt` when the caller swaps minions
   (`convex/calls.ts:52-60`), but the _skill count_ is derived from
   the minion. Without a re-roll the skill result would be stale. We
   therefore generate a fresh roll set whenever the head call's
   `minionId` changes. Same-minion replace stays the documented no-op.

3. **Roll sets must be immutable, append-only, and addressable by id.**
   This satisfies three constraints simultaneously:
   - notes need to "freeze" the roll set live at note-creation time;
   - replace-in-place must keep older roll sets intact (a Note made
     before the swap still references the pre-swap rolls);
   - "more rolls added conditionally in the future" reduces to
     "another optional field or another `extras` array entry on the
     same row" rather than a schema rewrite.

4. **One row per roll set, not one row per individual die.** Every
   "becoming the head" event always emits the skill+chaos pair
   atomically; future conditional dice will be authored alongside
   them. A single `callRollSets` row captures the full bundle as a
   transactional unit. Future conditional rolls go into an `extras`
   array of `{ kind, name, value, result? }` objects on the same row.
   `kind` is the machine-readable discriminator (e.g. `"luck"`,
   `"morale"`), `name` is the **required** human-readable display
   label (e.g. `"LUCK"`, `"MORALE"`); both are mandatory so every
   rendered die — present or future — always has a caption. This is
   extensible without migration.

5. **GM-only at the data boundary, not just the UI.** Rule 24 demands
   server-side enforcement. Roll data must therefore be stripped from
   any response served to a non-GM viewer — even when the underlying
   row exists. This is enforced inside the existing query handlers
   (`activeCalls`, `listNotesForTarget`, etc.), not the React layer.

6. **Notes capture the roll-set id, not a snapshot.** Roll-set rows
   are immutable, so a foreign key is sufficient and avoids
   duplicating data. Players never see the field — the API filters it
   out for non-GM viewers.

7. **Brutalist dice = numerals in a hard-bordered cell.** The theme
   forbids pip ornament, rounded corners, and gradient shadows
   (`THEME.md:62-91`). The two existing visual primitives that map
   to "two-state success/failure" are the riot-red top-border on
   `.card.drawback` (`src/index.css:986-994`) and the mint accent
   strip used on links/buttons. Reusing them keeps the dice in-style
   and avoids new CSS tokens. Skill and Chaos cells get hard-coded
   captions (`"SKILL"`, `"CHAOS"`); every extras cell takes its
   caption from the **required** `name` field on the row, with no
   fallback path.

8. **Natural-1 is a universal failure rule.** A `1` on any die — skill,
   chaos, or any future extras — must render with the failure
   treatment. The cleanest way to enforce this is to compute the
   result _at write time_ in the helper: skill failure becomes
   `roll === 1 || roll <= skillCount`, chaos gains an optional
   `chaosResult` field set to `"failure"` exactly when `chaosRoll
=== 1`, and any extras item with `value === 1` has its `result`
   coerced to `"failure"` regardless of what the caller passed. The
   UI applies the same rule defensively so any old row, or any future
   code path that bypasses the helper, still renders correctly. This
   keeps the rule self-documenting in the data, audit-friendly, and
   immune to surface drift.

## Implementation Plan

### Backend: schema

- [ ] Task 1. Add a `callRollSets` table to `convex/schema.ts`. Each
      row represents one "becoming-the-head" event for a specific call
      and is immutable once written. Fields: - `gameId: v.id("games")` — for per-game scoping and indexes. - `callId: v.id("calls")` — owning call. - `minionId: v.id("minions")` — snapshot of the called Minion at
      roll time (so a later replace-in-place doesn't invalidate
      historical roll sets attached to old Notes). - `skillRoll: v.number()` — integer 1–6. - `skillCount: v.number()` — non-negative integer, snapshot of
      `minions.skills.length` at roll time (the threshold). - `skillResult: v.union(v.literal("success"), v.literal("failure"))`
      — derived once via `(skillRoll === 1 || skillRoll <=
    skillCount) ? "failure" : "success"`; persisted so display
      logic never re-derives it. - `chaosRoll: v.number()` — integer 1–6. - `chaosResult: v.optional(v.union(v.literal("success"),
    v.literal("failure")))` — set to `"failure"` exactly when
      `chaosRoll === 1`; otherwise omitted (the rendered cell is
      neutral). Optional rather than required because chaos is
      intentionally unscored except for the natural-1 case;
      `"success"` is reserved for any future use that wants to
      explicitly mark a non-1 chaos as a pass, but v1 never
      writes that value. - `extras: v.array(v.object({ kind: v.string(), name:
    v.string(), value: v.number(), result:
    v.optional(v.union(v.literal("success"), v.literal("failure"))) }))`
      — empty for v1; reserved for future conditional rolls. The
      `name` field is **required, non-empty, trimmed, 1–24
      characters**; this guarantees every extras die has a display
      caption. `kind` remains the stable machine-readable
      discriminator. `result` is optional, but the helper
      guarantees that whenever `value === 1` it is set to
      `"failure"` (Task 3). - `createdAt: v.number()`. - `createdReason: v.union(v.literal("became_head"),
    v.literal("minion_replaced"))` — audit aid; distinguishes the
      two trigger paths in Tasks 4–6.
      Indexes: - `by_call_created` on `["callId", "createdAt"]` — used to fetch
      the most-recent roll set for a given call in O(1). - `by_game_call` on `["gameId", "callId"]` — supports per-game
      listings and bulk reads in `activeCalls`.

- [ ] Task 2. Extend the `notes` table at `convex/schema.ts:159-182`
      with an optional `attachedRollSetId: v.optional(v.id("callRollSets"))`.
      Optional because (a) Notes against `game` and `syndicate` targets
      never carry rolls, (b) Notes against a Minion that is **not**
      currently the head of the queue do not carry rolls, and (c)
      historical Notes pre-dating this feature have no roll set. No
      new index is required — Notes are already queried by their
      target indexes, and the field is read-after-fetch.

### Backend: roll generation helper

- [ ] Task 3. Add a private helper `generateRollSetForCall` in a new
      module `convex/lib/rolls.ts` (mutation-context only, since it
      uses `Math.random()`). Signature conceptually:
      `(ctx: MutationCtx, args: { callId, reason: "became_head" |
  "minion_replaced", extras?: Array<{ kind, name, value, result? }>
  }) => Promise<Id<"callRollSets">>`. The helper: 1. Loads the call, throws if missing or `isActive === false`
      (defensive — the helper must never run for an inactive call). 2. Loads the minion via `call.minionId`. If missing (a deletion
      race), returns without inserting and logs a console warning
      — the caller is in a "queue is broken" state anyway. 3. Computes `skillRoll = 1 + Math.floor(Math.random() * 6)` and
      `chaosRoll = 1 + Math.floor(Math.random() * 6)`. 4. Computes `skillCount = minion.skills.length` and applies the
      **natural-1 rule** to derive
      `skillResult = (skillRoll === 1 || skillRoll <= skillCount)
     ? "failure" : "success"`. Notes: under the existing
      minion-skill invariant (1–5 skills) the `=== 1` branch is
      redundant, but it is included unconditionally so the rule is
      self-documenting and survives any future relaxation of that
      invariant. 5. Derives `chaosResult = chaosRoll === 1 ? "failure" :
     undefined`. Stored as-is via `v.optional(...)`. 6. **Validates extras (if any)**: for each item, asserts
      `kind` is a non-empty trimmed string, asserts `name` is a
      trimmed non-empty string of length 1–24 (rejects with a
      descriptive error otherwise), asserts `value` is an integer
      in `[1,6]`. Trims `kind` and `name` before persisting. Then
      **applies the natural-1 rule**: if `value === 1`, override
      `result` to `"failure"` (regardless of what the caller
      passed); otherwise pass `result` through unchanged. v1
      passes no extras, but the validator and the natural-1
      coercion must be in place from day one so future callers
      cannot bypass either invariant. 7. Inserts a `callRollSets` row with `gameId = call.gameId`,
      the validated `extras`, `createdAt: Date.now()`,
      `createdReason: reason`, and returns the new id.
      Centralising this logic guarantees the same code path is used
      from both mutation entry points and from any future conditional
      roll add-on.

### Backend: trigger sites

- [ ] Task 4. In `addOrReplaceCall` (`convex/calls.ts:22-70`), add
      roll generation at the two sites that mutate the head: - **Empty-queue insert** (`convex/calls.ts:62-68`): after the
      `ctx.db.insert("calls", …)` returns, immediately call
      `generateRollSetForCall(ctx, { callId, reason: "became_head"
    })`. The new call is by definition the head because the queue
      was empty at the start of the transaction (the existing
      check at `convex/calls.ts:44-50` already enforces "no other
      active call for this player," and a queue-wide head check is
      unnecessary because the new call's `createdAt` is by
      definition the newest only when no others exist).
      Important nuance: Players other than the caller may have
      active calls older than this one. In that case the caller's
      new call is **not** the head and rolls must NOT be generated.
      The helper should therefore explicitly query the
      `by_game_active_time` head and skip generation if the new
      call's `_id` is not the current head. - **Replace-in-place** (`convex/calls.ts:52-60`): when
      `existing.minionId !== args.minionId`, after the patch, check
      whether `existing._id` is the current head via the
      `by_game_active_time` index. If it is, generate a new roll
      set with `reason: "minion_replaced"`. Otherwise no rolls
      (the call is mid-queue; rolls will be generated later when it
      becomes the head — see Task 5).
      Same-minion replace remains the documented no-op
      (`convex/calls.ts:53-56`).

- [ ] Task 5. In `removeCall` (`convex/calls.ts:76-92`), after the
      patch that soft-deletes the head call, look up the new head via
      `by_game_active_time` (oldest active in the same game). If a
      next call exists, generate a roll set for it with
      `reason: "became_head"`. If the queue is now empty, no-op.
      Note: only the GM may invoke this mutation, but the helper does
      not depend on caller role — it's the queue state that drives
      the roll, not who removed the call.

- [ ] Task 6. Edge case: if the GM removes a **mid-queue** call (not
      the head), no head change occurs and no rolls fire. Verify this
      is the natural behaviour of Task 5's "look up the new head"
      step — the head is unchanged, so the helper is called for the
      same call that already has a roll set. To avoid generating a
      duplicate roll set in that case, Task 5 must compare the
      removed call's `_id` to the pre-patch head's `_id` and only run
      generation when the removed call **was** the head. Document
      this guard explicitly in code comments.

### Backend: query exposure (GM-only)

- [ ] Task 7. Extend `getCurrentCallDetails`
      (`convex/calls.ts:151-209`) to additionally fetch the most-recent
      `callRollSets` row for the head call (via `by_call_created`,
      `order("desc")`, `take(1)`) and return it under a new
      `rolls: { skillRoll, skillCount, skillResult, chaosRoll,
  chaosResult, extras } | null` field, where `chaosResult` is the
      optional natural-1 marker (Task 3 step 5) and `extras` is the
      array of `{ kind, name, value, result? }` objects (faithfully
      passed through — `name` is non-empty by Task 3 and `result` is
      `"failure"` whenever `value === 1`). Already GM-gated by
      `requireGameGm` at `convex/calls.ts:155`, so no extra auth
      check needed. The field is `null` when the helper hasn't yet
      inserted (race-only) so the UI can render a "rolling…"
      placeholder if necessary.

- [ ] Task 8. Extend `activeCalls` (`convex/calls.ts:97-138`) to
      conditionally include the latest roll set per call. Implementation: - Compute `isGm = game.gmId === userId` (already done at
      `convex/calls.ts:102`). - For non-GMs, return the existing shape unchanged — **no
      `rolls` field is added at all** so the wire format never
      leaks the existence of rolls to Players. - For GMs, augment each item with
      `rolls: { skillRoll, skillCount, skillResult, chaosRoll,
    chaosResult, extras } | null`, fetched via the same per-call
      lookup as Task 7.
      Use a single bulk read (one `.collect()` per game with a
      `Map<callId, RollSet>`) rather than N individual reads, to keep
      the query's read budget within Convex's transaction limits as
      queues grow.

- [ ] Task 9. Update `listNotesForTarget` (`convex/notes.ts:158-231`)
      so that for GM viewers each returned note additionally includes
      a `attachedRolls: { skillRoll, skillCount, skillResult,
  chaosRoll, chaosResult, extras } | null` field, joined from the
      note's `attachedRollSetId`. For non-GM viewers the field is
      omitted entirely (same wire-format guarantee as Task 8). The
      note's own `attachedRollSetId` is also omitted from non-GM
      responses so a Player cannot discover which Notes carry rolls.

- [ ] Task 10. `getNoteCountsForGameView` (`convex/notes.ts:238-275`)
      requires no change — it counts visibility, not roll attachment.

### Backend: note creation freezes the live roll set

- [ ] Task 11. In `createNote` (`convex/notes.ts:50-131`), when
      `targetKind === "minion"` and the target Minion is currently the
      head of the queue's `minionId`, look up the head call's most
      recent roll set and persist its id as `attachedRollSetId` on the
      new note. Concretely: 1. Use the `by_game_active_time` index to fetch the head call
      for `args.gameId`. 2. If the head exists and `head.minionId === args.targetMinionId`,
      look up the latest `callRollSets` row for `head._id` via
      `by_call_created`. 3. If a roll set is found, include `attachedRollSetId` in the
      `ctx.db.insert("notes", …)` payload. 4. Otherwise insert without the field.
      This snapshot happens server-side and does not depend on the
      author's role — a Player who notes a head Minion has the
      roll-set id stamped onto their note row, but the API will not
      surface it to them (Task 9). Only the GM ever sees it.

### Frontend: dice display primitives

- [ ] Task 12. Create a small presentational component
      `RollSetDisplay` co-located in `src/components/RollSetDisplay.tsx`.
      Props: `{ rolls: { skillRoll, skillCount, skillResult, chaosRoll,
  chaosResult?, extras: Array<{ kind, name, value, result? }> } |
  null, size?: "sm" | "md" }`. Renders square cells side-by-side
      using existing utility classes only.

      **Universal natural-1 rule (defence-in-depth).** Before
      rendering any individual cell, the component derives an
      effective result per slot:

      - skill: `skillRoll === 1 ? "failure" : skillResult`
      - chaos: `chaosRoll === 1 ? "failure" : (chaosResult ?? null)`
      - each extra: `value === 1 ? "failure" : (result ?? null)`

      This re-applies the same rule the helper enforces (Task 3) so
      any historical row, any future caller that bypasses the helper,
      or any future schema relaxation still renders a `1` as a
      failure. The helper guarantees the rule at write time; the UI
      guarantees it at read time; both layers must remain consistent.

      - **Skill cell**:
        - Heading caption "SKILL" using `.section-label` styling
          (`THEME.md:170-180`).
        - Big mono numeral for `skillRoll` (`var(--font-mono)` with
          `tabular-nums`, sized via `1.5rem` for `md`, `1.05rem` for
          `sm`).
        - Footer caption "vs §{skillCount}" (the `§` glyph echoes the
          `.bulletin` motif in `THEME.md:117-121`).
        - On effective result `"failure"`, apply the same riot-red
          top rule as `.card.drawback` (`src/index.css:986-994`)
          plus a danger badge "FAIL" beneath the numeral. On
          `"success"`, apply a 4px mint bottom-strip (mirroring the
          link-underline treatment at `src/index.css:101-118`) plus
          a black-on-white badge "PASS".
      - **Chaos cell**:
        - Heading caption "CHAOS".
        - Big mono numeral for `chaosRoll`.
        - On effective result `"failure"` (i.e. `chaosRoll === 1`),
          apply the same riot-red top rule and "FAIL" badge as the
          skill cell. Otherwise neutral: 2px black border on white,
          no decoration.
      - **Extras**: render each item in `rolls.extras` as an
        additional cell in the same row, using
        `extra.name.toUpperCase()` (the **required** display name
        from the row) as the caption, with the same numeral
        treatment. The component MUST NOT fall back to `kind` for
        the caption — the schema (Task 1) and helper (Task 3)
        guarantee `name` is always present and non-empty. Apply the
        fail/pass decoration based on the effective result derived
        above; an extra with no `result` and `value !== 1` is
        rendered neutrally like Chaos.
      - When `rolls === null`, render two cells with an em-dash
        placeholder and the caption "PENDING" — this state is rare
        (Task 7's race-only branch) but is well-defined.
      All borders, shadows, and typography come from existing tokens.
      No new CSS file. If a small amount of layout glue is required
      (e.g. `.roll-set { display: inline-flex; gap: 0.5rem; }`), add
      it to the existing `src/index.css` near the other utility
      blocks rather than a sidecar stylesheet.

- [ ] Task 13. Re-use `RollSetDisplay` in three call surfaces: - **Current Call section** (`src/pages/GameDetailPage.tsx:2057-2294`,
      the GM-only deep-dive): place the `md` variant in the header
      row beside the "Remove call" button (or below the
      caller→minion line if the row would otherwise wrap on narrow
      viewports). Data source is the new `data.rolls` field from
      Task 7. Already GM-gated. - **Right-rail Call Queue** (`CallQueueRail`,
      `src/pages/GameDetailPage.tsx:1403-1483`): for each entry,
      render the `sm` variant **only when `isGm === true`** and the
      item is the **first** in the active list (the FIFO head). The
      rail stays compact for Players because no `rolls` field is
      present in their `activeCalls` response (Task 8). - **Mobile bottom sheet** (`BottomStrip`,
      `src/pages/GameDetailPage.tsx:1974-2041`) inherits this for
      free because it renders `CallQueueRail`.
      Non-head queue entries do not display rolls — the user's
      requirement is "every call that makes it to the top," so
      mid-queue calls have no rolls until they advance.

### Frontend: dice on Notes

- [ ] Task 14. Update `NoteList` (the layout-agnostic list extracted
      in the prior Current-Call plan) to render a `RollSetDisplay`
      with size `sm` next to a note's metadata when the joined
      `attachedRolls` field is present. Position it inline with the
      note timestamp/author byline so it is visually subordinate to
      the note body but unmistakable. Because non-GM viewers never
      receive the field (Task 9), no client-side role check is
      needed; the absence of the field naturally hides the dice.

- [ ] Task 15. Verify the popover-mounted `NoteList`
      (`src/components/NoteIcon.tsx`) and the inline
      `NoteList` in the Current Call section
      (`src/pages/GameDetailPage.tsx:2258-2290`) both pick up the dice
      display automatically through Task 14 — no per-call-site change
      should be needed beyond the shared component.

### Backend: tests

- [ ] Task 16. Extend `convex/calls.test.ts` with a new `describe`
      block "dice rolls" covering: - Adding a call to an empty queue inserts exactly one
      `callRollSets` row with `createdReason === "became_head"`,
      `gameId` matching, `minionId` matching the called minion,
      `skillCount` equal to that minion's skill array length, both
      rolls in `[1,6]`, and `skillResult` consistent with the
      natural-1-aware rule
      `(skillRoll === 1 || skillRoll <= skillCount) ? "failure" :
    "success"`. Loop the assertion over many runs to exercise
      every roll outcome without coupling to a seed. - **`chaosResult` is `"failure"` exactly when `chaosRoll === 1`**
      and is `undefined`/absent for every other chaos value. Verify
      across many runs. - **Skill rolls of `1`** are always recorded as failure — even
      in a hypothetical row with `skillCount === 0` (which the
      production schema disallows but the helper must still handle
      defensively). Use a unit test on the helper or an injected
      minion-skill snapshot to exercise this branch. - Adding a second call (different player) while a head call
      exists does **not** insert a new roll set. - Removing the head call inserts a new roll set for the next
      call with `createdReason === "became_head"`. - Removing a mid-queue call does **not** insert a roll set
      (Task 6 guard). - Replace-in-place on the head call to a _different_ minion
      inserts a new roll set with `createdReason ===
    "minion_replaced"`, and the new row's `skillCount` reflects
      the new minion's skills. - Replace-in-place on the head call to the _same_ minion
      inserts no new roll set (idempotent — confirms the no-op
      branch at `convex/calls.ts:53-56` is preserved). - Replace-in-place on a non-head call inserts no roll set. - `activeCalls` returns `rolls` (including `chaosResult` and
      `extras`) for the GM and _omits_ the field entirely for
      Players and outsider non-participants (assert via
      `Object.prototype.hasOwnProperty`). - `getCurrentCallDetails` returns the latest roll set when
      multiple exist for the head (multiple replace-in-place
      cycles).

- [ ] Task 17. Extend `convex/notes.test.ts` with a new `describe`
      block "dice attachment" covering: - Creating a Minion-target Note while that Minion is the head
      stamps `attachedRollSetId` onto the note row, and the GM's
      `listNotesForTarget` response includes a populated
      `attachedRolls` object whose values match the live roll set
      — including `chaosResult` when the chaos die was a `1`. - Creating a Minion-target Note for a Minion that is _not_ the
      head leaves the field unset. - Creating a Note before any roll set exists for the head
      (defensive race path) leaves the field unset. - A Note attached to a roll set survives a subsequent
      replace-in-place (the original roll-set row is not mutated;
      the Note still resolves to the original rolls). - A Player viewing their own Note that has an
      `attachedRollSetId` does NOT see `attachedRolls` in the API
      response and does NOT see `attachedRollSetId` either. - A GM viewing the same Note DOES see both.

- [ ] Task 18. Verify with one extra test that `getNoteCountsForGameView`
      is unaffected by the new field (regression guard against an
      accidental schema validator drift).

- [ ] Task 19. Add a focused unit test in `convex/lib/rolls.test.ts`
      (or a sub-block inside `convex/calls.test.ts`) for the helper's
      extras-validation and natural-1 branches: - Helper rejects an extras item with empty `name` (after trim). - Helper rejects an extras item with `name` longer than 24
      chars. - Helper trims and persists `kind` and `name` (e.g. `"  luck "`
      → `"luck"`). - Helper rejects `value` outside `[1,6]` or non-integer. - **Helper coerces `result` to `"failure"` when `value === 1`,
      even if the caller passed `"success"` or omitted the field.**
      Verify the persisted row carries `result: "failure"`. - **Helper passes `result` through unchanged when `value !== 1`**
      — both when the caller specifies it and when the caller
      omits it (omitted stays omitted).
      The v1 trigger sites pass no extras, but exercising the
      validator now locks the "every die has a name" and "natural-1
      is failure" invariants for future callers.

### Frontend: tests / smoke checks

- [ ] Task 20. If the project gains a UI test harness, add a smoke
      test that the right rail and Current Call section render dice
      cells for the GM and not for Players, that any extras cells
      always show their `name` caption, and that a die showing `1`
      in any slot (skill, chaos, or an extras stub) renders the
      failure treatment (riot-red top rule + "FAIL" badge) regardless
      of any other context. Otherwise rely on the backend coverage
      above plus manual verification per the criteria below — same
      trade-off accepted by the prior Current Call plan
      (`plans/2026-04-27-2026-04-27-current-call-rail-section-v1.md:325-327`).

## Verification Criteria

- A GM playing through a queue of three calls sees a fresh skill+chaos
  pair appear on each of the three calls **in turn** as they reach
  the head, and the rolls disappear from the rail when the GM removes
  the call.
- The skill roll shows as **failure** iff `skillRoll === 1 ||
skillRoll <= skillCount`, and as **success** otherwise; the visual
  treatment matches Task 12.
- A GM who replaces the head call's minion sees an entirely new pair
  of rolls, with `skillCount` reflecting the new minion. Replacing
  with the _same_ minion is a no-op; rolls do not change.
- A GM who removes a _mid-queue_ call sees the head's rolls unchanged
  (no spurious re-roll).
- **Any die in any slot showing `1` is rendered with the failure
  treatment** (riot-red top rule + "FAIL" badge): a chaos `1` flips
  the otherwise-neutral chaos cell to failure; an extras `1` flips
  that extra's cell to failure regardless of any caller-supplied
  `result`. A die showing 2–6 keeps its existing rendering rules
  (skill compares to `skillCount`; chaos is neutral; extras follow
  their stored `result`).
- A Player viewing the same game sees no dice cells anywhere — neither
  in the rail, the bottom sheet, the Current Call section (which they
  never see anyway), nor inline on Notes. The `activeCalls` and
  `listNotesForTarget` payloads they receive contain no `rolls` /
  `attachedRolls` / `attachedRollSetId` / `chaosResult` fields
  whatsoever.
- A Player notes the head Minion of the queue. The Note appears
  immediately in the popover and Current Call section; the GM viewing
  that same Note sees the dice that were live at the moment the Note
  was authored — including any natural-1 failure highlights. After
  the Player or GM advances the queue (causing a fresh roll set), the
  older Note **still** displays its original rolls (immutable history).
- A non-participant calling `activeCalls`,
  `getCurrentCallDetails`, `listNotesForTarget`, or `createNote`
  receives the existing "not a participant" / "not the GM" errors.
  None of the new code paths leak rolls.
- All dice cells render with hard 2px black borders, no border
  radius, no blurred shadows, mono numerals with tabular-nums; the
  failure variant uses the riot-red top rule and the success variant
  uses the mint accent strip — matching `THEME.md:62-91` and the
  verification checklist at `THEME.md:182-198`.
- **Every die rendered anywhere on the page has a non-empty caption.**
  The Skill and Chaos cells have hard-coded captions; every extras
  cell renders the row's required `name` field uppercased, with no
  fallback path to a raw `kind` slug. A schema-level attempt to
  insert an extras item with an empty `name` is rejected by the
  helper (Task 3, Task 19).
- All new and existing backend tests pass under `npm test` /
  `vitest`, and `npx convex codegen` (or equivalent) succeeds with
  the new schema.

## Potential Risks and Mitigations

1. **Race between queue mutation and read.** A subscriber querying
   `activeCalls` between the `ctx.db.insert("calls", …)` and the
   `generateRollSetForCall` call could briefly see `rolls === null`.
   Mitigation: both operations live in the same mutation transaction
   in Convex, so external readers will only see the post-commit state
   in which both rows exist. The `null` branch of `RollSetDisplay`
   (Task 12) exists purely as defence-in-depth for replay/error paths.

2. **Replace-in-place ambiguity.** Reasonable readers may disagree on
   whether a same-call minion swap counts as "becoming the top."
   Mitigation: Task 4 documents the chosen interpretation
   ("minion change → re-roll") and the `createdReason` field
   (`became_head` vs `minion_replaced`) records which path produced
   each row, supporting future audit and policy reversal.

3. **Wire-format leakage.** A naive `if (isGm) item.rolls = …` left
   the field on the response object as `undefined`, which JSON-omits
   but Convex value-encodes — Players would still see the _key_
   absent in JSON but present in the validator. Mitigation: build
   the response object via two distinct shapes (Player branch never
   constructs the field at all). Add the explicit `hasOwnProperty`
   assertion in Task 16's tests to catch regressions, including for
   the new `chaosResult` field.

4. **Test flakiness from `Math.random()`.** Asserting exact roll
   values is impossible. Mitigation: tests assert _invariants_
   (range, count, derived `skillResult`, `chaosResult` per the
   natural-1 rule, schema fields, presence of the row), not specific
   outcomes; the property-style "loop and verify the rule holds for
   every run" pattern in Task 16 catches skew without coupling to a
   seed.

5. **Schema migration on existing notes.** `attachedRollSetId` is
   optional, so old notes remain valid. Mitigation: explicit
   `v.optional(...)` validator and no backfill required. Confirm by
   running tests against the existing fixtures unchanged.

6. **Read-budget growth in `activeCalls`.** Each call now triggers a
   roll-set lookup. Mitigation: Task 8's "single bulk read into a
   Map" approach keeps reads at O(active calls + active rolls in the
   game), which is bounded by the per-player at-most-one-active-call
   rule (`convex/calls.ts:44-50`).

7. **Brutalist constraints regressed.** Adding a new visual primitive
   risks introducing `border-radius` or blurred shadows.
   Mitigation: Task 12 names the existing tokens to reuse and forbids
   new design tokens; Task 20's verification step explicitly walks
   the `THEME.md:182-198` checklist on the new component.

8. **Future conditional rolls colliding with `extras`.** A future
   feature might want richer per-roll data (e.g. context, links).
   Mitigation: `extras` items have `kind`, `name`, `value`, and
   optional `result`; new optional keys can be added without
   breaking existing rows. The non-optional `name` field guarantees
   any future addition is forced to ship a display caption rather
   than relying on a downstream UI fallback.

9. **Unnamed extras leaking into UI.** A future contributor could be
   tempted to push an extras item with a placeholder `name`.
   Mitigation: Task 3's helper validates `name` (trimmed,
   1–24 chars), Task 12 forbids any `kind`-fallback render path, and
   Task 19 covers the rejection case in tests so the regression
   surfaces immediately.

10. **Natural-1 rule drift.** A future contributor could change the
    skill/chaos formulas in only one of the two layers (helper or
    UI), producing rows whose stored `*Result` disagrees with the
    rendered cell. Mitigation: the rule is stated in three places
    that fail loudly when one drifts — Key finding 8 (intent),
    Task 3 (write-time), Task 12 (read-time) — plus tests in
    Tasks 16 and 19 assert the invariant on every roll outcome and
    on the helper's extras coercion. Task 12's defence-in-depth
    rederivation also keeps the visual correct even when a stored
    `chaosResult` is missing on a legacy row.

## Alternative Approaches

1. **Compute rolls on demand from a deterministic seed.** Rather than
   storing roll-set rows, derive rolls from a hash of `(callId,
generation, gameSecret)`. Trade-off: still need a row to record
   "generation" per top-event and to attach to Notes, so the storage
   savings are nil; deterministic seeds also bake replay-debuggability
   into a security-sensitive surface. Rejected.

2. **One row per individual die.** Use a `callRolls` table with one
   row per die kind. Trade-off: simpler in the abstract, but every
   read needs to assemble a "set" by scanning multiple rows ordered
   by a synthetic `setId`, which complicates Note attachment (the
   Note would need either an array of ids or a join through a
   `callRollSets` parent that we're trying to avoid in the first
   place). Rejected — the parent row is unavoidable.

3. **Snapshot rolls onto the Note instead of FK.** Store the literal
   `skillRoll`/`chaosRoll` on the note row. Trade-off: duplicates
   data, complicates schema changes when `extras` grows, and makes
   it harder to surface the same rolls in multiple UI places (Note
   list and Current Call header) without divergence. Rejected.

4. **Generate rolls from a query (lazy).** Have `getCurrentCallDetails`
   roll dice when no row exists. Trade-off: queries in Convex are
   deterministic and cannot use `Math.random()` reliably; would also
   create a roll on every cold subscription. Rejected — generation
   must remain a mutation-side effect.

5. **Surface rolls only in the Current Call section, not the rail.**
   Simpler UI. Trade-off: the user explicitly asked for rolls on
   "the call," and the rail is the canonical call display in the
   mobile drawer where the GM doesn't have the main column visible.
   Rejected — both surfaces matter.

6. **Show rolls to Players too.** Trade-off: directly contradicts
   the user's "Only the GM can see dice rolls, at any time, no
   matter where they are in the game" requirement. Rejected.

7. **Keep `name` optional with a UI fallback to `kind`.** Lighter
   schema, but lets unnamed extras leak past the helper into the UI
   and forces every render site to carry the same fallback logic.
   Rejected — the user's explicit ask is that extras "be named also,"
   and locking it at the schema is the only single source of truth
   that survives future contributors.

8. **Apply the natural-1 rule purely in the UI layer.** Keep the
   schema unchanged; let `RollSetDisplay` interpret any `1` as
   failure. Trade-off: no `chaosResult` schema bloat, but the rule
   is no longer self-documenting in the data, audit/export surfaces
   would need to re-derive it, and any non-React surface (future
   mobile clients, Convex dashboards, server-side reports) would
   silently miss the rule. Rejected — encoding it at write time
   keeps the data honest and the UI's defence-in-depth check
   remains as a safety net.

9. **Promote `chaosResult` to required.** Always set it explicitly,
   either `"failure"` (on a 1) or `"success"` (otherwise). Trade-off:
   removes the UI's "neutral" chaos rendering — every chaos result
   would carry a pass/fail marker even though the user only asked
   to highlight `1`s. Rejected; v3 keeps chaos neutral except for
   the natural-1 case to match the user's exact request.
